name: domain
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  libs:
    path: ../libs
  models:
    path: ../models
  injectable: ^2.7.1+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  injectable_generator: ^2.9.1
  build_runner:
  freezed:
  riverpod_generator:
  riverpod_lint:
  custom_lint: ^0.8.1

flutter:
  uses-material-design: true
  generate: true
